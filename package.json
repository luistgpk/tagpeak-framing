{
  "name": "tagpeak-framing",
  "version": "1.0.0",
  "description": "Survey application for email framing effects study",
  "main": "index.html",
  "scripts": {
    "dev": "node build.js && vercel dev",
    "build": "node build.js",
    "start": "vercel dev",
    "build:css": "tailwindcss -i ./assets/input.css -o ./assets/tailwind.css --minify"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.23",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.19",
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
